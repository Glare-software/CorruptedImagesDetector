    ${appVersionTitle}

    Directory process report

Processed directory "${currentDir}" at ${dateTime}

${renamingInfo}
<#list files as file>
${file_index + 1}. ${file}
</#list>

See total report at ${fullReportPathAndName}


--
Source code available at https://github.com/fdman85/BrokenImagesDetector
